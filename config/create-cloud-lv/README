# Management OS LVs:
vg0-boot
vg0-root
vg0-swap

# Home directory LV:
vg0-home

# Cloud LVs:
vg0_<release>[_<arch>]

# Create a new cloud LV:
0) Verify update-grub is patched (/usr/local/sbin)
1) Boot into management OS

# Remove a cloud LV:
